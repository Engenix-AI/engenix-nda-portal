ENGENIX BLACK DIAMOND V4.1 — FORM FIX

Cause:
The new private-access drawer did not include the required "challenge" field expected by the existing /api/demo Worker. The Worker therefore rejected submissions.

Fixed:
- Added required challenge textarea
- Added source metadata
- Restored honeypot field
- Added visible success/error status area
- Preserved the existing /api/demo pipeline

Fast deploy:
Replace index.html and styles.css only.
script.js and worker.js do not need to change.
